using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class koma_here : MonoBehaviour
{
    public int [] here = new int[2];
    public void Here(int i, int j)
    {
        here[0] = i;
        here[1] = j;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
